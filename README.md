### Simple minecraft installer (script) for Fedora linux, which uses legacy-launcher and steam as proton provider

*Install command:*
```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/z-Eduard005/fedora-mc-installer/main/mc-installer.sh)"
```

