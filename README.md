### Simple minecraft installer (script) for linux, which uses legacy-launcher and steam as proton provider

*Install command:*
```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/z-Eduard005/linux-mc-installer/main/installer.sh)"
```
